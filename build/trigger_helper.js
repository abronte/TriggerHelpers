// Generated by CoffeeScript 1.6.2
(function() {
  var Apptentive, Display, Event, Logger, Notification, Parse, Platform, Prefs, Request, isTrigger, root, _base, _base1, _nextTick, _ref, _ref1, _ref10, _ref2, _ref3, _ref4, _ref5, _ref6, _ref7, _ref8, _ref9,
    __indexOf = [].indexOf || function(item) { for (var i = 0, l = this.length; i < l; i++) { if (i in this && this[i] === item) return i; } return -1; };

  isTrigger = window.forge !== void 0;

  root = (_ref = (_base = typeof exports !== "undefined" && exports !== null ? exports : window).forge) != null ? _ref : _base.forge = {};

  root.isTrigger = function() {
    return isTrigger;
  };

  _nextTick = function(func) {
    if (func != null) {
      return setTimeout(func, 1);
    }
  };

  Display = (function() {
    var f, _i, _len, _ref1;

    function Display() {}

    _ref1 = ['forceLandscape', 'forcePortrait', 'allowAny'];
    for (_i = 0, _len = _ref1.length; _i < _len; _i++) {
      f = _ref1[_i];
      Display.prototype[f] = function(success, error) {
        return typeof success === "function" ? success() : void 0;
      };
    }

    return Display;

  })();

  if ((_ref1 = root.display) == null) {
    root.display = {};
  }

  if ((_ref2 = (_base1 = root.display).orientation) == null) {
    _base1.orientation = new Display;
  }

  Event = (function() {
    var ADD_LISTENERS;

    ADD_LISTENERS = ['menuPressed', 'orientationChange', 'connectionStateChange', 'messagePushed', 'appPaused', 'appResumed'];

    function Event() {
      var namespace, _i, _len;

      for (_i = 0, _len = ADD_LISTENERS.length; _i < _len; _i++) {
        namespace = ADD_LISTENERS[_i];
        this[namespace] = {
          addListener: function() {}
        };
      }
    }

    Event.prototype.backPressed = {
      addListener: function() {},
      preventDefault: function(success, error) {
        return typeof success === "function" ? success() : void 0;
      }
    };

    return Event;

  })();

  if ((_ref3 = root.event) == null) {
    root.event = new Event;
  }

  Platform = (function() {
    function Platform() {}

    Platform.prototype.mobile = function() {
      return false;
    };

    Platform.prototype.web = function() {
      return true;
    };

    Platform.prototype.android = function() {
      return false;
    };

    Platform.prototype.ios = function() {
      return false;
    };

    Platform.prototype.orientation = {
      portrait: function() {
        return true;
      },
      landscape: function() {
        return false;
      }
    };

    Platform.prototype.connection = {
      connected: function() {
        return true;
      },
      wifi: function() {
        return true;
      }
    };

    return Platform;

  })();

  if ((_ref4 = root.is) == null) {
    root.is = new Platform;
  }

  Logger = (function() {
    var LEVELS;

    LEVELS = {
      critical: {
        level: 1,
        methodName: "error"
      },
      error: {
        level: 2
      },
      info: {
        level: 3
      },
      warning: {
        level: 4,
        methodName: "info"
      },
      debug: {
        level: 5
      }
    };

    function Logger() {
      var level, methodName, name, _ref5;

      this.logLevel = 5;
      for (name in LEVELS) {
        _ref5 = LEVELS[name], level = _ref5.level, methodName = _ref5.methodName;
        this[name] = function(msg) {
          if (this.logLevel >= level) {
            return console[methodName != null ? methodName : name](msg);
          }
        };
      }
    }

    Logger.prototype.level = function(level) {
      if (level !== void 0) {
        this.logLevel = level;
      }
      return this.logLevel;
    };

    return Logger;

  })();

  if ((_ref5 = root.logging) == null) {
    root.logging = new Logger;
  }

  Notification = (function() {
    function Notification() {}

    Notification.prototype.alert = function(title, text, success, error) {
      var result;

      if (!((title != null) && (text != null))) {
        console.warn("Title and message text are required!");
      }
      result = confirm("" + title + "\n\n" + text);
      return _nextTick(success);
    };

    Notification.prototype.create = Notification.prototype.alert;

    Notification.prototype.confirm = function(title, body, positive, negative, success, error) {
      var result;

      result = confirm("" + title + "\n\n" + body + "\n\n(" + negative + "/" + positive + ")");
      return _nextTick(function() {
        return typeof success === "function" ? success(result) : void 0;
      });
    };

    Notification.prototype.toast = function(body, success, error) {
      var style, toast;

      style = {
        position: "absolute",
        top: "10%",
        'z-index': 99,
        width: "33%",
        'margin-left': "33%",
        'text-align': "center",
        background: "#666",
        color: "#FFF",
        padding: "1em 0.4em",
        'border-radius': "4px"
      };
      toast = $("<div>" + body + "</div>").css(style);
      $("body").prepend(toast);
      toast.fadeIn(200).delay(1000).fadeOut(1000, function() {
        return toast.remove();
      });
      return _nextTick(success);
    };

    /*
    Not implemented:
    notification.setBadgeNumber(number, success, error)
    notification.showLoading(title, body, success, error)
    notification.hideLoading(success, error)
    */


    return Notification;

  })();

  if ((_ref6 = root.notification) == null) {
    root.notification = new Notification;
  }

  Parse = (function() {
    function Parse() {}

    Parse.prototype.installationInfo = function(success) {
      return nextTick(function() {
        return success({
          id: 'dev-browser'
        });
      });
    };

    Parse.prototype.push = {
      subscribe: function(channel, success) {
        var subscriptions;

        subscriptions = this._channels();
        if (__indexOf.call(subscriptions, channel) < 0) {
          subscriptions.push(channel);
        }
        this._setChannels(subscriptions);
        return nextTick(success);
      },
      unsubscribe: function(channel, success) {
        var i, subscriptions;

        subscriptions = this._channels();
        i = subscriptions.indexOf(channel);
        subscriptions = subscriptions.slice(0, i).concat(subscriptions.slice(i + 1));
        this._setChannels(subscriptions);
        return nextTick(success);
      },
      subscribedChannels: function(success) {
        var _this = this;

        return nextTick(function() {
          return success(_this._channels());
        });
      },
      _channels: function() {
        var _ref7;

        return JSON.parse((_ref7 = localStorage.getItem('forge-helper:subscriptions')) != null ? _ref7 : "[]");
      },
      _setChannels: function(channels) {
        return localStorage.setItem('forge-helper:subscriptions', JSON.stringify(channels));
      }
    };

    return Parse;

  })();

  if ((_ref7 = root.parse) == null) {
    root.parse = new Parse;
  }

  Prefs = (function() {
    function Prefs() {}

    Prefs.prototype.get = function(key, callback) {
      var _this = this;

      return _nextTick(function() {
        return typeof callback === "function" ? callback(localStorage.getItem(_this._toForgeKey(key))) : void 0;
      });
    };

    Prefs.prototype.set = function(key, val, callback) {
      localStorage.setItem(this._toForgeKey(key), val);
      return _nextTick(callback);
    };

    Prefs.prototype.keys = function(callback) {
      var i, key, keys, _i, _ref8;

      keys = [];
      for (i = _i = 0, _ref8 = localStorage.length - 1; 0 <= _ref8 ? _i <= _ref8 : _i >= _ref8; i = 0 <= _ref8 ? ++_i : --_i) {
        key = localStorage.key(i);
        if (this._isForgeKey(key)) {
          keys.push(this._fromForgeKey(key));
        }
      }
      return _nextTick(function() {
        return callback(keys);
      });
    };

    Prefs.prototype.clear = function(key, callback) {
      localStorage.removeItem(this._toForgeKey(key));
      return _nextTick(callback);
    };

    Prefs.prototype.clearAll = function(callback) {
      var _this = this;

      return forge.prefs.keys(function(keys) {
        var key, _i, _len;

        for (_i = 0, _len = keys.length; _i < _len; _i++) {
          key = keys[_i];
          localStorage.removeItem(_this._toForgeKey(key));
        }
        return _nextTick(callback);
      });
    };

    Prefs.prototype._toForgeKey = function(key) {
      return "forge:" + key;
    };

    Prefs.prototype._fromForgeKey = function(key) {
      return key.replace(/^forge:/, "");
    };

    Prefs.prototype._isForgeKey = function(key) {
      return key.match(/^forge:/);
    };

    return Prefs;

  })();

  if ((_ref8 = root.prefs) == null) {
    root.prefs = new Prefs;
  }

  Request = (function() {
    function Request() {}

    Request.prototype.get = function(url, callback) {
      return $.ajax({
        type: "GET",
        url: url,
        dataType: "jsonp",
        processData: false,
        success: callback
      });
    };

    Request.prototype.ajax = function(params) {
      var oldError,
        _this = this;

      oldError = params.error;
      if (oldError != null) {
        params.error = function(e) {
          return oldError(_this._transformErrorArgument(e));
        };
      }
      return $.ajax(params);
    };

    Request.prototype._transformErrorArgument = function(webError) {
      return {
        message: webError.statusText,
        type: "EXPECTED_FAILURE",
        subtype: null,
        statusCode: "" + webError.status,
        content: webError.responseText
      };
    };

    return Request;

  })();

  if ((_ref9 = root.request) == null) {
    root.request = new Request;
  }

  Apptentive = (function() {
    function Apptentive() {}

    Apptentive.prototype.setApiKey = function(key) {};

    Apptentive.prototype.setAppID = function(username) {};

    Apptentive.prototype.setInitialUserName = function(username) {};

    Apptentive.prototype.setInitialUserEmailAddress = function(email) {};

    Apptentive.prototype.addCustomData = function(data, key) {};

    Apptentive.prototype.removeCustomData = function(key) {};

    Apptentive.prototype.showRatingFlowIfConditionsAreMet = function() {};

    Apptentive.prototype.logSignificantEvent = function() {};

    Apptentive.prototype.presentMessageCenter = function() {
      return forge.logging.debug("Apptentive message center opened");
    };

    Apptentive.prototype.unreadMessageCount = function(cb) {
      return nextTick(function() {
        return cb(0);
      });
    };

    Apptentive.prototype.unreadMessageCountChanged = {
      addListener: function(cb) {}
    };

    Apptentive.prototype.hasSurveyAvailableWithNoTags = function(cb) {
      return nextTick(function() {
        return cb(false);
      });
    };

    Apptentive.prototype.hasSurveyAvailableWithTags = function(cb) {
      return nextTick(function() {
        return cb(false);
      });
    };

    Apptentive.prototype.surveyBecameAvailable = {
      addListener: function(cb) {}
    };

    Apptentive.prototype.presentSurveyControllerWithNoTags = function() {
      return forge.logging.debug("Apptentive survey presented (no tags)");
    };

    Apptentive.prototype.presentSurveyControllerWithTags = function(tags) {
      return forge.logging.debug("Apptentive survey presented (tags: " + tags + ")");
    };

    Apptentive.prototype.surveyWasSent = {
      addListener: function(cb) {}
    };

    return Apptentive;

  })();

  if ((_ref10 = root.apptentive) == null) {
    root.apptentive = new Apptentive;
  }

}).call(this);
